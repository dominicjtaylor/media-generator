# ============================================================
# Instagram Carousel Automation — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ------------------------------------------------------------
# LLM provider — "anthropic" (default) or "openai"
# ------------------------------------------------------------
LLM_PROVIDER=anthropic

# Anthropic (used when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (used when LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# ------------------------------------------------------------
# Optional: disable the review pass (saves one LLM call per generation)
# ------------------------------------------------------------
# REVIEW_ENABLED=false

# ------------------------------------------------------------
# Optional: log level
# ------------------------------------------------------------
# LOG_LEVEL=DEBUG

# ------------------------------------------------------------
# Lummi API (required for headings_text_image template style)
# Get your key at: https://lummi.ai/developers
# ------------------------------------------------------------
LUMMI_API_KEY=

# ------------------------------------------------------------
# Optional: Instagram auto-post (stub — future use)
# ------------------------------------------------------------
# INSTAGRAM_ACCESS_TOKEN=
# INSTAGRAM_ACCOUNT_ID=
